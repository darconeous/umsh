.DS_Store
xcuserdata/
*.xcuserstate
.swiftpm/
/old
/target
/docs/ux/book
/site/public
/.umsh
*.identity
build
__pycache__/
/.claude/settings.json
/.vscode/settings.json
/*.code-workspace

# Region database working directories. `build` above already covers
# regions/build/; these are the download and release trees.
/regions/vendor
/regions/dist
/tools/regiondb-build/.venv
# The fixture's build report is a build output whose timings change on every
# run; the database's content hash is what the tests actually assert.
/regions/tests/fixture/build-report.json
